-- SQL script that lists tables in a database in a MySQL server
-- The database name will be passes as an argument of mysql command as such:
-- cat 3-list_tables.sql | mysql -h localhost -u root -p `mysql`
SHOW TABLES
