-- The script prints the full description of the table `first_table` from...
-- a database in a MySQL server
-- The database name is passed as an argument of the MySQL command as such:
-- * cat %-full_table.sql | mysql -h localhost -u root -p <database>
SHOW CREATE TABLE first_table;
